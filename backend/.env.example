# Optional overrides — sensible defaults are used if unset.
LOG_LEVEL=INFO
UPLOAD_DIR=uploads
MAX_FILE_SIZE_BYTES=52428800

# Required starting Phase 7 (Claude integration) — not used yet.
ANTHROPIC_API_KEY=
